/** 항구 boundary GeoJSON으로 만든 deck.gl 마스크 그룹/레이어 id. */
export const LOCAL_MASK_ID = 'local-boundary-mask';

/** detail(z=11) 그룹을 base(z=6)보다 위에 그리기 위한 zIndex 가산값. */
export const LOCAL_ZINDEX_OFFSET = 5;
